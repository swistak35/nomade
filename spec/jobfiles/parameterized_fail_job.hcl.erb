job "failingpayload" {
  datacenters = ["<%= template_variables[:datacenter] %>"]

  type = "batch"

  parameterized {
  }

  task "hi" {
    driver = "docker"

    config {
      image = "<%= image_name_and_version %>"

      command = "/bin/false"
    }

    resources {
      cpu    = 500 # 500 MHz
      memory = 256 # 256MB
    }
  }
}

